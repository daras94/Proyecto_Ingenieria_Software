<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.uvportal.uahgii.uvportal">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".Login"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ElegirCarrera"
            android:label="@string/title_activity_elegir_carrera"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".ElegirAsignaturas"
            android:label="@string/title_activity_elegir_asignaturas"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".IntroducirUsuario"
            android:label="@string/title_activity_introducir_usuario"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".CuentaAlumno"
            android:label="@string/title_activity_cuenta_alumno"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".VerExpediente"
            android:label="@string/title_activity_ver_expediente"
            android:theme="@style/AppTheme.NoActionBar"></activity>
    </application>

</manifest>